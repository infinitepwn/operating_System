### 引言

存储器的层次结构包括

- 高速缓存
- 内存
- 外存

cache和内存具有易失性:掉电即丢失，它们直接与处理器交互

> 帕金森定律：不管存储器有多大，程序都可以把它填满

### 无存储抽象

最简单的存储抽象就是无抽象

程序中的地址就是真实的内存地址

![image-20260521171230682](/Users/infinite/Library/Application Support/typora-user-images/image-20260521171230682.png)

#### 运行多个程序

##### 多线程编程

因为同一个进程中的所有线程对同一内存映像都可见

但是没有存储器抽象的系统不大可能支持线程抽象的功能

##### 切换程序（进程）的时候整个程序（进程）在内存—硬盘之间切换

交换

##### 特殊硬件

 一些处理器支持使用内存保护单元（MPU）

把内存划分为若干区域，每个区域设置访问权限，进程拥有自己的访问权限，只能访问权限相对应的内存

##### IBM 360中的模型：

 内存被划分为2KB的块，每个块被分配一个4位的保护键

 保护键存储在CPU的特殊寄存器中。

 PSW（Program Status Word，程序状态字）寄存器中存有一个4位码

 只有PSW中的4位码和保护键相同时，该内存才可以访问

 PSW只有特权模式下可以访问

使用内存保护单元（MPU）的重要缺陷

本质原因：绝对物理地址

#### 静态重定位

### 地址空间

#### 基址寄存器和界限寄存器

 每次一访问内存，处理器自动把基址寄存器值加到地址值上；同时检查地址是否等于或大于界限寄存器值

#### 交换技术

#### 空闲内存管理

##### 使用位图的存储管理

##### 使用链表的存储管理

### 虚拟内存

### 页面置换算法

